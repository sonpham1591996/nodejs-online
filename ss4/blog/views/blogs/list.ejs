<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List blogs</title>
    <style>
        table {
            width: 60%;
            margin: 2rem auto;
            border-collapse: collapse;
        }


        tr,
        th,
        td {
            border: 1px solid black;
            text-align: center;
        }

        h1 {
            text-align: center;
        }

        .btn-create {
            display: flex;
            justify-content: center;
        }

        .ml-2 {
            margin-left: 10px;
        }
    </style>
</head>

<body>
    <h1>List Blogs</h1>
    <div class="btn-create">
        <a href="/blogs/creation-form">Create blog</a>
    </div>
    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Category</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% blogs.forEach((blog)=> { %>
                <tr>
                    <td>
                        <a href="/blogs/<%= blog.id %>/detail">
                            <%= blog.id %>
                        </a>
                    </td>
                    <td>
                        <%= blog.name %>
                    </td>
                    <td>
                        <%= blog.category %>
                    </td>
                    <td>
                        <a class="ml-2" href="/blogs/<%= blog.id %>/update-form">Edit</a>
                        <a class="ml-2" href="/blogs/<%= blog.id %>/delete">Delete</a>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>
</body>

</html>